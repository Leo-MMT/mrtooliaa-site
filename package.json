{
  "name": "mrtooliaa-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio estático de MR ToolIAA — landings consolidadas (empresa + MediTurno + Ferret-Quote).",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "verify:csp": "node scripts/verify-csp-hashes.mjs",
    "verify:links": "node scripts/verify-links.mjs",
    "verify": "npm run build && npm run verify:csp && npm run verify:links"
  },
  "dependencies": {
    "astro": "^5.7.10",
    "@astrojs/tailwind": "^6.0.2",
    "tailwindcss": "^3.4.17"
  }
}
